<%= form_for @idea, html: {id: "idea-form" } do |f| %>
  <%= f.label :title %>
  <%= f.text_field :title, value: @idea.title, id: "title-input" %>

  <%= f.label :body %>
  <%= f.text_field :body, value: @idea.body, id: "body-input" %>

  <%= f.submit id: "idea-create-button" %>
<% end %>
